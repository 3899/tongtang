# 更新日志

## 2.3.2 · 2026-07-29

### 变更（旧名清理收尾）
- **会话 Cookie 默认名 `qj_session` → `tongtang_session`**：初版名缩写的最后一个活跃残留。**升级后所有用户需重新登录一次**（旧名 Cookie 不再被读取，随 30 天有效期自然过期）；自定义过 `SESSION_COOKIE_NAME` 的部署不受影响
- 名称清理收尾：package.json 包名 `ha-family-control` → `tongtang`（并补齐漏更的版本号）、测试临时目录前缀同步；NAS 侧已删除檐下时代的 `eaves-builder` 旧构建器、文件桥执行器在线改名 `tongtang-runner`。**尚余（择机与 dev 栈迁移一起做）**：`docker-compose.yml` 的 compose 项目名 `ha-family-control` 与数据卷名 `home_console_data`——改名=容器/网络/卷全部重建，存量自建部署会与老数据卷脱钩，须迁移方案配套

## 2.3.1 · 2026-07-29

### 变更（内部名称统一为「同堂」，无功能变化）
- 项目初版名「栖居 / Home Console」在代码内部的全部残留清理：后端日志命名空间 `qiju.*` → `tongtang.*`（10 个模块）、PWA 离线缓存名更换（激活时自动清除旧缓存）、模块描述与样式注释同步；「檐下」经全库核查已无任何残留
- **数据库文件名**：新部署改为 `data/tongtang.db`；存量部署升级后自动继续沿用原文件 `home-console.db`——无需迁移、无任何影响。特意不做启动时改名：api 与 HomeKit 侧车并发启动，SQLite 还有 `-wal`/`-shm` 伴生文件，跨进程原子改名无法保证，文件名统一的收益远小于竞态风险。备份命令文档同步改为通配 `*.db*`，两种文件名都覆盖
- 复查补遗：清掉「檐下」的英文名残留——发行部署文档里已失效的 `EAVES_IMAGE_NS` 指令、裸机 systemd 模板描述；会话 Cookie 默认名 `qj_session`（栖居缩写）**特意保留**，改名会强制所有用户重新登录，需要新名字的用 `SESSION_COOKIE_NAME` 环境变量自定义

## 2.3.0 · 2026-07-29

### 新增（Matter 桥 · 全类型）
- **Matter 桥**：互联平台新增 Matter 页签——一座桥可同时接入苹果家庭 / Google Home / Alexa 等全部 Matter 平台（multi-admin）。与 HomeKit 原生桥平行的第三种互联模式：每用户一座桥、独立配对码、各平台操作经同堂权限校验并记入审计，权限收回自动摘除设备
- 架构：`tongtang-mt` 侧车容器（node20 + matter.js 0.16，host 网络）；控制与配置统一经 api 内部端点（HMAC 派生令牌），权限/审计单点在 python；桥/勾选/权限变化经 matter_rev 对账重建，配对身份保留；映射层独立为 `mapping.mjs` 纯逻辑模块（MAPPING_VERSION 参与桥签名，映射语义变更自动重建防 iPhone 吃旧缓存）
- **全类型接入（对齐原生 HomeKit 桥）**：灯（开关/调光/色温/彩色 HS）、开关/插座、窗帘（含百叶倾角，Matter 与 HA 开合方向换算）、门锁、空调（制冷/制热/自动/除湿/送风 + 设定点按实体量程钳制）、热水器（30-80°C 量程）、风扇/新风净化器（按名称识别）、水阀、温湿度/照度/气压传感器、人体占用/门窗/水浸、烟雾与燃气/CO 报警（SmokeCoAlarm 特性分装）、空气质量全家桶（PM2.5/PM10/CO₂/CO/NO₂/O₃/VOC 浓度 + AQI 六档分级）、情景/脚本/自动化触发开关（automation 走 trigger 不误碰启用开关）；Matter 无对应类型的降级：加湿器/扫地机/割草机/媒体播放器→动作开关，安防面板/门铃事件不入候选
- **控制回传双机制**：开关/调光/温控/风扇走 attribute 监听（endpoint.set 回声经 offline 上下文抑制）；锁/窗帘/阀是 iOS 发 Matter 命令，覆盖 behavior 命令方法回传（LockDoor/GoToLiftPercentage/Open）——python 端 `translate_control` 把 Matter 语义契约翻成 call_service 词汇
- **发行版接线（两份 compose 预置，注释开关二选一）**：方式 A 自动编排——api 设 `MT_IMAGE` + 挂 docker.sock，建桥自动创建 `tongtang-mt` 侧车、删光自动回收、镜像/环境变化自动重建；方式 B 静态侧车——取消 `mt` 服务注释即长驻，api 端零开关（未设 MT_IMAGE 自动识别常驻模式）。发布镜像 `jeesa/tongtang-mt`（esbuild 压缩单文件产物，publish.sh 含压缩校验），健康检查为心跳文件新鲜度（镜像内置）
- 侧车日志脱敏：matter.js 默认日志级别收敛 warn（未配对桥的 NOTICE 会打印配对码明文），排障用 `MT_LOG_LEVEL=info/debug` 临时放开；dev compose 默认 info
- 全类型自测：`test-bridge.mjs` 以合成实体覆盖 35 种形态跑真 ServerNode（Conformance 校验 + applyState 冒烟），并修复自测揪出的 5 处：百叶窗 type 特性冲突、门锁反义位图 alwaysSet 位段、彩色灯缺 HueSaturation 特性、占用传感器缺 occupancySensing behavior、空气质量枚举特性门控
- 配对码由 matter.js 计算回写（MT: 二维码 + 11 位手动码），测试 VID 0xFFF1（开源软件桥无认证实体，添加时"未认证设备"提示属正常，操作手册已说明）
- 发版前复查（第四轮，12 发现全修，映射版本升至 2）：automation 触发磁贴静息恒灭（跟随"启用态"会造成死点击与语音失效）；温控 deadband 单位修正（0.1°C：25 而非 250）；方式 A 回收侧车会残留旧配对存储 → 桥身份标记（created_at）自动清除防僵尸配对；起桥失败自动重试 + 离线节点周期对账重建（此前 rev 不变即永不重试）；热水器 Off 拨杆接通 turn_on/off（此前是假开关）；Auto 模式双设定点改走 target_temp_low/high（此前 80ms 合并队列互吞必丢一个）；气压按规范 ×10（0.1 kPa 单位）；风扇档位序列 0=Off/Low/Med/High（此前声明 Off/High 却写 Low/Med）；无位置能力窗帘不再强加百分比滑杆（按 supported_features 探测 PositionAware）；attribute 监听真解绑（matter.js Observable.on 无返回值，须 off(listener)）；APP_SECRET 短于 16 位时两端行为不一致 → 侧车明示退出 + api 一次性日志提示；侧车 api 轮询加 10s 超时（docker restart 不处理 unhealthy，卡死曾是永久僵尸）

### 修复（存量代码全量复查 · 首轮轮换，7 处真伤）
- **涂鸦状态上报任务被 GC 中途回收**：`notify_state` 派生的上报任务只被 asyncio 弱引用，无强引用时可能在完成前被垃圾回收、设备状态推送静默丢失。改为 `_inflight_pushes` 集合强引用至完成（`add_done_callback` 自动摘除）
- **涂鸦监督器读库瞬时失败触发 MQTT 重连风暴**：`_load_state` 抛异常（`database is locked` 等）时原先拿空集去对账，空 `wanted` 会 cancel 掉全部在线网关任务再重连——表现为周期性涂鸦 MQTT 重连风暴。现捕获异常、保持现有连接、跳过本轮对账、5 秒后重试
- **巴法监督器同类隐患**：读库瞬时失败同样会拿空集 cancel 全部在线桥再重连，巴法反复多连可致账号异常。加同样的守卫（保持连接、跳过本轮、稍后重试）
- **场景"首页显示"开关落库失败不回滚**：`toggleSceneHome` 乐观改开关后若偏好 PUT 失败，UI 会停在错误态、与服务器不符（其余设备偏好早有回滚，唯独场景漏了）。现失败回滚乐观态并提示
- **删除表计缺二次确认**：删除表计按钮直接删库、无 `confirm`，与面板其他删除动作（成员/家庭/分组/实例均有确认）不一致，易误删且下级表计会静默升顶。补二次确认
- **锁乐观态瞬间误显"已解锁"**：锁走 `{on}` 通道但卡片按 `state==="locked"` 字面判定，上锁瞬间（WS 回状态前）乐观态被写成通用 `on` 会误显"已解锁"。现乐观态直接给锁终态字面值（`locked`/`unlocked`），与 HA 上报一致
- **Matter 侧车删除实例后 WS 状态源泄漏**：`config.instances` 只驱动"新增/更新"，被移除实例的 WS feed（含 pingTimer/重连定时器）会永远重连一个不存在的 HA、泄漏连接与句柄。`ensureFeed` 循环后加回收对账：不在配置内的 feed 一律 `stop()` + 摘除

### 修复（存量代码深读复查 · 第 2 轮轮换：数据迁移 / 实时推送 / 能耗，8 处真伤）
- **实体改名会连带清空 Matter 桥的设备勾选**：HA 实体改名后同堂把旧键迁到新键，但迁移子表清单漏了 `matter_devices`——该表对 `devices(entity_id)` 带 `ON DELETE CASCADE`，迁移末尾按旧键删 `devices` 行时会静默级联删掉用户在 Matter 桥里的勾选（HomeKit/米家/涂鸦三表首轮已覆盖，Matter 是新表遗漏）。已补入迁移子表清单
- **慢/半开客户端会冻结整条事件摄取**：设备状态广播此前对每个前端 WebSocket 串行 `await` 发送，任一半开连接（拔网、息屏后台）阻塞就会卡住其后所有客户端乃至 HA 事件摄取循环。改为并发扇出 + 每帧 5 秒发送超时，慢客户端只拖累自己、超时即剔除
- **心跳与广播可能并发写同一 WebSocket**：连接级心跳与设备广播是两条协程，可能同时向同一 socket 写帧（Starlette send 非并发安全，会协议错乱报错）。加每 socket 串行发送锁，心跳与广播共享同一锁排队
- **读实例表瞬时失败触发重连风暴**：实时监听监督器每 30 秒对账「该连哪些 HA 实例」，若这一刻读实例表恰逢 `database is locked` 等瞬时失败，原先会拿到空集、把所有附加实例误判为「已删除」而全部取消重连——表现为周期性重连风暴。现捕获异常、保留既有任务、跳过本轮、下轮再试
- **权限查询阻塞事件循环**：设备广播前查「哪些成员可见该设备」是同步 SQLite，此前直接在事件循环里执行，高频事件下会周期性卡住整个推送与摄取。改为放线程池（与状态落库一致）
- **api 与侧车并发初始化会崩库迁移**：api 与 tongtang-hk/mt 侧车启动时都会跑 `init_db` 加列迁移，两边同时 check-then-ALTER 存在竞态——一方刚加完列、另一方紧接着 `ALTER` 抛 `duplicate column` 使启动崩溃。抽 `_add_column` 助手吞掉重复加列（失败方沿用对方写好的最终 schema）
- **能耗配置表重建会丢燃气/用水实体且无原子性**：`energy_configs` 一处历史 schema 重建用了 `executescript`（自动提交、半途崩溃无法回滚），且重建后的新表遗漏 `gas_entity_ids`/`water_entity_ids`——升级路径上用户配的燃气/水表实体会被清空。改为显式事务（`BEGIN IMMEDIATE`）包裹整个重建、新表补齐两列并带回填兜底；另加残留自愈——历史版本重建中途崩溃留下的 `energy_configs_old` 残表会在下次启动按共有列自动搬回数据并清理
- **能耗历史与成本按 Wh 表会差 1000 倍**：能耗曲线接口对 HA 统计增量未做单位归一，而汇总口径已按 kWh 换算——传感器原生单位是 Wh/MWh 时历史曲线与成本会相差 1000 倍（kWh 表无影响）。补单位归一（Wh×0.001、MWh×1000），与汇总口径对齐
- **发版前终审复核（独立代理复读上述 8 处修复的实现）**：抓获并修正 2 处实现缺口——层级模式的总量序列漏传单位归一参数（Wh 总表的总量/成本仍会差 1000×）；重建的原子性依赖了错误前提（sqlite 传统模式下 DDL 自动提交，"事务内"并不成立），已改显式事务。另加固 3 处：读实例表失败的跳过路径补告警日志（原静默空转）、删除表计失败补错误提示、并发加列容错收窄为仅吞「列已存在」（其余数据库故障快速失败交由重启自愈，不再静默留下缺列 schema）

### 修复
- **涂鸦监督器空配置告警刷屏**：管理员未配置涂鸦 API Key 时，`_load_state` 早退返回值形状与成功路径不一致（第三项少了二元组包装），监督器解包失败每 30 秒刷一条"读取涂鸦网关失败：not enough values to unpack"——生产日志实测抓获，已修
- **Matter 空调设定点间距回滚**：空调/温控器只写单侧设定点（如 28°C 制热）会撞另一侧旧值的 2.5°C 强制间距校验（AutoMode deadband），matter.js 回滚整笔 set 连室温/模式一起丢——日志巡检真机抓获，现凡写必成对联动保间距（热水器 Heating-only 语义不变），自测样本改为 28.5°C 制热作回归

## 2.2.5 · 2026-07-28

### 修复（Zigbee2MQTT 兼容性专项：真机审计 + 全部 4363 台设备定义机器核对）
- **CO₂/CO 浓度传感器勾不进**：候选白名单只认非法值 "co2"/"co"，补齐 HA 正名 `carbon_dioxide`/`carbon_monoxide`（旧值保留兼容）——影响所有标准集成，不止 Z2M
- **事件按键被指令流轰炸**：调光遥控器按住调光（brightness_move/stop）、旋钮旋转（rotate）、Aqara FP1 区域事件（region_enter/leave）、魔方状态（wakeup/inactivity）此前都被兜底成"单击"连环触发 HomeKit 按钮——列入忽略清单；Hue 系 initial_press/release 幻影触发同步修复（short_release=单击主信号）；全语料核对补漏：`held`=长按、厂商拼错的 `tripple_*`=三击、`rotation/start_rotating` 旋转流与字面量 `none/unknown` 忽略
- **震动传感器**（vibration）从占用兜底改为动作传感器（对齐 HA）

### 修复（HA 官方词汇表全量核对：54 传感器类 / 28 二元类 / 全部状态机 × 映射代码）
- **PM2.5 浓度传感器自 2.2.0 起实际未接入**：HAP 特征名是 `PM2.5Density`（带点），代码误写为下划线拼法，KeyError 被容错吞掉、实体静默消失——发布前复查抓获，已修并补回归测试
- **PM10/VOC/NO₂/CO 浓度传感器自 2.2.0 起实际未接入**：构建方法存在但漏接分发链，实体被静默丢弃（当年测试未走完整链路）——已接上并补回归测试
- 空调 `preheating/defrosting`（预热/除霜）动作现正确显示为"制热中"
- 门/窗类电动开合（cover 的 door/window 设备类）改用 HomeKit 专属 Door/Window 服务形态（此前一律按窗帘）
- 二元域 CO 报警器正名（字面量 `co`）补入映射（原生桥此前兜底成占用）；其余核对通过：二元 28 类对齐 HA 官方桥、锁 jammed 卡死态、安防度假/自定义布防早已覆盖
- 按键分类前向兼容：Matter 多击 `multi_press_3..18/multi_press_end` 降级双击（此前误判单击）、`multi_press_ongoing` 与 HA 新标准词汇 `press_start` 列入忽略
- 车库门名称救回收敛为"集成未给 device_class 时才生效"——显式 curtain 类的"车库卷帘"不再被误转车库门；AQI/臭氧/二氧化硫/ppb-VOC 四类仅原生桥模式提供勾选（HA 自带桥吃不下，避免废勾选项）

### 新增
- 臭氧（ozone）、二氧化硫（sulphur_dioxide）浓度传感器接入——HomeKit 原生有对应密度特征
- ppb/ppm 计的 TVOC（`volatile_organic_compounds_parts`，Z2M 常见）按异丁烯当量换算后接入 VOC 浓度
- AQI 空气质量指数实体（`aqi`）按国标分段映射 HomeKit 五档
- 车库门名称救回（车库/garage/gate）——Z2M 的 cover 官方不带 device_class；门铃名称救回补英文 doorbell
- 映射版本升至 7，已配对桥自动刷新

## 2.2.4 · 2026-07-27

### 新增
- **组合配件子服务显示实体名**：PDU/多灯等整机配件展开后，子控件不再被 iOS 自动编号为"插座 1-8/灯 1-2"，而是显示各实体名称（外层磁贴仍为设备名）。原理：iOS 16+ 只认 ConfiguredName 特征做子服务显示名，映射层已全量补齐；映射版本升至 6，已配对桥自动刷新。家庭 App 里手动改的子控件名会在桥结构变化时还原为实体名，建议改名在 HA/同堂侧进行
- **HAP 端口冲突自愈**：桥端口被外部进程占用（EADDRINUSE）时，侧车自动探测下一个空闲端口、回写桥配置并重新起桥——配对身份保留，iOS 经 mDNS 自动跟到新端口；探测在侧车（host 网络）内进行，避免 api 容器网络命名空间误判
- **`HK_ADDRESS` 环境变量**：强制指定原生桥 mDNS 通告地址。宿主机装有 WireGuard/Tailscale 等虚拟网卡或多网卡时，默认按"默认路由源 IP"通告可能选错网卡（表现为配对最后一步失败、已添加配件一直"未连接"），填 NAS 局域网 IP 即可；方式 A 自动透传给侧车，方式 B 在 hk 服务 environment 里设置

### 修复
- **方式 A 侧车环境变量变化不生效**：自动编排此前只按镜像判断侧车是否过期，事后在 api 上新增/修改 `HK_ADDRESS` 等变量会被静默忽略；现按环境签名比对，变量变化自动重建侧车（配对在数据卷中，无损）
- **智能插座被识别成开关**：绿米/ZHA 等集成的插座 switch 实体常缺 `device_class: outlet`，现按名称自动救回（插座/插头/plug/socket 等）为插座形态；优先级：同堂显示类型 > HA device_class > 名称识别
- 首页"安全/扬声器/水"状态胶囊激活时图标一片空白：`status-icon` 样式缺失 blue 色调规则，补齐

## 2.2.3 · 2026-07-27

### 修复
- **可燃气体等非标二元传感器接不进原生桥**：国产集成常见 device_class 缺失/非标（problem、safety、空值）导致实体不出现在候选列表。现原生桥接受任何二元传感器：按名称自动识别形态（燃气/煤气/瓦斯→CO 检测告警、烟雾/烟感→烟雾、水浸/漏水→漏水），无法识别则回退占用传感器（对齐 HA homekit 兜底）；映射版本升至 4，已配对的桥自动刷新结构

## 2.2.2 · 2026-07-26

### 修复
- **tongtang-hk 侧车健康检查**：侧车此前继承镜像自带的 8000 端口探测（守护进程不跑 Web 服务）恒显示 unhealthy——改为心跳文件检查（守护进程每对账周期刷新，健康检查看 60 秒新鲜度）；自动编排创建与 compose 静态方式均已覆盖

## 2.2.1 · 2026-07-26

### 原生 HomeKit 桥 · 侧车两种开通方式（compose 预置注释开关）
- **方式 A · 自动编排**：取消 `docker.sock` 挂载行注释后，**第一次建桥自动创建 tongtang-hk 侧车**（host 网络、复用本镜像与数据卷），全部桥删除后自动回收，镜像升级后启动自检自动以新镜像重建侧车
- **方式 B · 静态侧车**：不愿挂 docker.sock 的用户取消 `hk` 服务整段注释 + `HOMEKIT_NATIVE: "1"`，由 compose 直接创建长驻侧车；双容器与一体化两份 compose 均已预置
- `HOMEKIT_NATIVE` 环境变量改为可选：未设置时按 docker.sock 是否挂载自动判定；`=1/=0` 仍可强制指定（静态部署/强制关闭）
- 安全说明：docker.sock 等同宿主机管理权，发行版中为可选叠加并注明 docker-socket-proxy 收紧方案

## 2.2.0 · 2026-07-26

### 新增：原生 HomeKit 桥（可选模式，设备整机接入）
- **不再受 HA「按实体拆配件」限制**：同堂直接承载 HAP 协议（HAP-python，随上游 pip 升级同步 iOS 兼容性），同一物理设备整机一块磁贴——**PDU 插排一块磁贴展开 8 口独立控制**、温湿度+电量合一、双模灯/多灯合并；热水器等 HA 桥不支持的域也可接入（映射为恒温器）
- **全类型映射**：灯（亮度/色温/彩色）、开关/插座、空调、窗帘（含无位置回退）、风扇（风速/摇摆）、门锁、加湿器、热水器、安防面板、扫地机/割草机、阀门与常见传感器；摄像头继续走 HA 侧
- **对照 HA homekit 全量补齐（强化超集）**：电视（信源切换/iOS 遥控器按键/音量）、门铃与无线按钮（事件实体 → iPhone 原生按铃/按键通知）、**情景/脚本/自动化接入**（一按即触发的开关，普通用户按可见性隔离）、车库门形态（含开合过程状态）、百叶斜倾角、空调风速滑条 + heat_cool 双设定点 + 湿度显示、除湿机方向、风扇正反转、CO/PM10/VOC/NO₂ 与燃气告警传感器、安防按设备支持特性限定可选模式；配件信息三件套（厂商/型号/序列号取自 HA 设备注册表，不再是 default）
- **权限与审计闭环**：家庭 App 里的每次操作实时复查同堂权限并记入审计（via homekit）——HA 桥模式做不到的能力
- **部署**：`docker compose -f docker-compose.yml -f docker-compose.homekit.yml up -d` 叠加 tongtang-hk 侧车（需 host 网络做 mDNS 广播；Docker Desktop 不适用，默认 HA 桥模式保底不变）；配对状态持久化在数据卷，重启不掉配对
- 多桥管理（按房间/类型自建命名、单桥 145 上限校验、挪桥确认）两种模式通用

### 互联平台 · HomeKit
- **多桥自动扩容**：单桥约 150 配件（HAP 规范上限）——现勾满 120 个自动为该用户开下一座桥，每座桥独立配对二维码；分配粘性：已接入设备永不在桥间挪动（挪桥会导致配件在家庭 App 里删除重建、丢失房间归属与自动化引用）
- **候选列表不再出现电池实体**：HA 会自动把同物理设备的电池并入配件电量属性，单独暴露只会多出一个孤立磁贴
- **手册注明实体拆分行为**：温湿度等一体设备在家庭 App 显示为多个配件属 HA homekit 集成官方模型（按实体暴露、无合并手段），家庭 App 房间视图会自动聚合气候数据

## 2.1.2 · 2026-07-24

### 体验
- **个性化实时预览**：个性化从居中弹窗改为底部浮动面板（手机为底部 sheet）——无遮罩不压暗，切换外观/风格/卡片形态/玻璃浓度时整页实时呈现效果，点面板外任意处收起（Apple：平行任务用非模态浮层，不打断预览对象）
- **控制浮窗滚动边缘最终形态**：放弃"标题垫色遮罩"（垫色必比半透明面板深，滚动时会显出深色帽，且毛玻璃方案存在 mask+backdrop-filter 引擎差异）——重构为固定标题 + 独立滚动区，滚动内容在标题下缘自身羽化溶解，全引擎表现一致

## 2.1.1 · 2026-07-21

### 体验 · Apple 设计规范整改（对照 Designing Fluid Interfaces / HIG 全面审查）
- **浮层对称退场（空间一致性）**：所有弹窗、设备控制浮窗、组合设备浮窗、图标选择器、门铃横幅、摄像头全屏，此前都是"动画进场、瞬间消失"——现按"从哪来回哪去"补齐镜像缓动的退场动画（桌面缩放淡出原路返回、手机 sheet 滑回底部、门铃横幅缩回顶部），动画结束才真正卸载
- **明暗/风格切换整页交叉淡化**：主题切换不再亮度硬跳（View Transitions API，不支持的浏览器自动直切）
- **调光滑条 1:1 跟手**：拖动竖向滑条时填充不再滞后手指（拖动中禁过渡，松手恢复平滑动画）
- **按压回弹补齐**：拆分按钮、移动端菜单项按下即时响应、松手平滑归位（此前松手硬弹）
- **触控命中区扩展**：图标按钮/开关的命中区域扩展至 ≥44px（视觉尺寸不变）

### 无障碍（减弱 ≠ 移除反馈）
- **减弱动效精细化**：`prefers-reduced-motion` 此前一刀切禁用全部动画与过渡——现仅禁用位移/缩放/循环类运动，保留有助理解的短时颜色与透明度渐变；浮层进出退化为轻淡入淡出
- **减弱透明度**：新增 `prefers-reduced-transparency` 支持——毛玻璃材质变实底、去模糊，七套主题经变量层统一生效
- **提高对比度**：新增 `prefers-contrast: more` 支持——材质实底 + 随明暗主题自适应的明确边框与更强的次级文字对比

## 2.1.0 · 2026-07-21

### 新增：互联平台 · HomeKit 专属桥
- 每位成员在「互联平台 → HomeKit」勾选设备保存，即自动在 HA 中创建只属于自己的 HomeKit 桥（托管 HA 内置 homekit 集成，零额外组件）；页面直出配对二维码与配对码，iPhone 家庭 App 扫码接入、Siri 直控
- 精确过滤：桥上有且只有勾选的设备（include 模式逐实体点名）；增删勾选与权限收回实时同步进家庭 App 且不破坏已配对状态；解除时自动删除 HA 侧条目，删除用户同步注销其桥
- 多 HA 实例按家各建一桥（桥是局域网设备，各家 iPhone 配各家的桥）；摄像头默认 H.264 直通不转码；兼容新旧两代 homekit 配置流（按各步实际 schema 自适应组装）
- 桥 reload 窗口的配对状态竞态处理：启动中 / 等待配对（出码）/ 已配对三态，杜绝误报

### 新增：能源
- **今日电量自动推算**：电表没有「每日归零」实体时「今日电量」留空即可，按「累计电量」实体推算当日增量（实时读数 − 当天 0 点基线；基线取 HA 长期统计、按天缓存，高频汇总零额外开销）；换表归零当天按 0 计、次日自愈

### 修复
- **实体改名迁移补全**：互联平台勾选表（小爱 / 涂鸦 / HomeKit）纳入改名迁移——实体被 HA 批量改名后，不再从米家 / 涂鸦 App / 家庭 App 消失

## 2.0.0 · 2026-07-19

### 新增：互联平台（米家/小爱 + 涂鸦双生态桥）
- **统一入口**：头像菜单新增「互联平台」，分页签管理各生态桥接；设备勾选与授权面板同款交互（搜索 + 实例/房间/类型级联筛选 + 全选筛选结果），弹窗底部保存按钮常驻；两套桥接均内置分步操作手册，亲友自助接入
- **小爱同学 · 巴法云**：每位用户绑定自己的巴法云私钥（一个巴法账号对应一个米家账号），在自己有权限的设备里勾选同步进米家，小爱同学语音控制。支持灯（亮度）、插座、开关、风扇（档位）、窗帘（开停关/开度）、空调（模式+温度）、温湿度传感器，热水器映射为开关；状态实时回显（`主题/up` 防指令回环）、语音操作记入审计（via xiaoai）、权限收回自动撤出米家；断线重连与远端主题对账自愈（只清理本桥命名模式的主题，绝不误删同账号下的其他设备）
- **涂鸦智能 · 虚拟网关**：管理员配置一次 API Key（tuya.ai）后，每位用户生成专属网关二维码、涂鸦智能 App 扫码绑定（官方 end-user 网关接口，一 Key 多用户实测，每网关 30 个子设备）。勾选的设备经 App 确认添加为**家庭首页顶层设备**（官方同款接入仪式、完整产品面板、按同堂房间自动归位）。品类映射逐 DP 对齐涂鸦云端规则包：灯（亮度/色温/彩色按实体能力声明）、开关/插座、风扇（风速/摇摆/正反转/预设）、新风/净化（按能力自动选品类）、窗帘（开停关/开度/工作状态）、空调（模式/温度/风速/预设/扫风/运行状态完整 9 DP）、热水器、加湿器、扫地机（启停/暂停/回充/寻找/吸力）；**温湿度计与门磁按物理设备整机接入**（温度+湿度+电量合并为一台）。实体不可用 ↔ 子设备离线同步；涂鸦 App 侧删除设备/网关自动感知；物模型签名机制——映射升级只重建声明变化的设备；指令执行前实时复查权限并记入审计（via tuya）

### 架构与可靠性
- 服务端为每个绑定维护一条 MQTT 长连接（监督器模式：断线指数退避重连、30 秒对账自愈）；桥接要求单实例运行
- 巴法云端点与涂鸦 TuyaLink 协议均经真机逐项探测验证；绑定响应丢失自动经拓扑查询按 clientId 找回，杜绝"云端已建、本地无记录"的悬挂态

## 1.1.8 · 2026-07-16

### 性能（显著降低卡顿）
- **反代 gzip**：`/api` 的 JSON 响应此前未压缩——设备列表实测 840KB → 33KB（约 25 倍），首屏与切换家庭的加载明显变快
- **REST 连接池**：对 HA 的每次请求不再重建连接（此前每次控制/快照都要重新 TCP+TLS 握手，远程 HTTPS 实例上单次可达数百毫秒）；连接复用后单请求约 3ms
- **同值心跳跳过**：传感器高频上报同一数值时不再触发界面重算——事件风暴期的掉帧来源
- **实时推送瘦身**：增量只发动态状态字段，静态字段（名称/房间/图标等）不再随每次变化重发

### 稳定性
- **实体改名迁移**（应对 HA 2026.5.4+ 「重建实体 ID」带区域前缀批量改名，core#173125）：以注册表 registry id 识别改名，授权/偏好/图标/显示为/家庭设备集/表计/能源配置全部无损跟随新 ID，不再被当作删除清零；迁移记入审计。升级后请先执行一次「同步实体与房间」
- **WebSocket 加固**：按规范以 id 关联命令响应（防错位）；单帧 30 秒超时（HA 卡死时同步不再悬挂）

## 1.1.7 · 2026-07-15

### 修复
- **大型 HA 同步失败（1009 message too big）**：实体注册表/状态列表单帧超过 WebSocket 客户端默认 1 MiB 上限时（集成较多的 HA 常见），同步与实时监听会被 1009 断开——已取消消息尺寸限制。此问题会导致首次配置向导报"无法连接 Home Assistant: sent 1009 frame exceeds limit"

## 1.1.6 · 2026-07-15

### 新增
- **摄像头大图**：实时画面浮窗新增「大图」按钮——铺满整个窗口（黑底、保持比例），顶部信息条显示摄像头名与模式，Esc / 点击背景 / 右上角 × 退出；大图期间小窗自动停流，不重复占用带宽
- **首次配置向导感知环境变量**：compose 里设置了 `HA_URL`/`HA_TOKEN` 时，向导自动预填地址并锁定、令牌栏显示"已由环境变量提供"，只需填管理员账号——不再出现"填了也不生效"的困惑

### 修复
- **连接 HA 报 401 的两类根因**：①地址与令牌输入在向导 / HA 设置 / 附加实例三处入口统一清洗首尾空白（粘贴带换行会让令牌失效）；②401 报错附带人话提示（检查令牌完整性与空白字符；设置了环境变量时明确提示以环境变量为准、修改需重建容器）

## 1.1.5 · 2026-07-12

### 新增
- **授权面板升级**：可用设备区新增 **HA 实例 / 房间 / 类型** 筛选与「只看已授权」开关；「全选筛选结果」一键勾选（合并语义，单个实体可点选取消）——先筛后全选再剔除的批量授权工作流。实例下拉始终显示：候选范围由被授权用户的家庭归属决定，属于多个家庭时可跨实例切换
- **「关于」弹窗**：个人菜单新增入口——项目简介、当前版本号（构建时自动注入）、GitHub 与 Docker Hub 链接
- **「个性化」弹窗**：外观（明亮/暗色/自动）、七套风格（带主题色板圆点）、方块/胶囊卡片（带形状预览）、玻璃浓度滑杆统一收编，iOS 分段控件视觉；桌面侧栏与移动菜单的内联外观控件移除，界面更清爽

### 修复
- 弹出菜单（个人菜单/家庭切换）点击页面其他位置不会自动收起的问题

## 1.1.4 · 2026-07-10

### 新增
- **组合卡拆分/合并**（对齐 HomeKit「作为单独板块显示」）：组合卡浮窗底部一键「拆分为独立卡片」，同设备多个实体各自成卡；任一实体浮窗 → 设置 → 「组合为一张卡」开关随时合并回去；按用户生效，纯展示层（HA 无需任何调整）。跨设备编组用管理后台「新建分组」
- **「显示为」类型切换**（对齐 HomeKit）：开关类实体可在 开关/插座/灯 三种显示类型间切换（浮窗设置 → 显示为）——图标、卡片点亮色、类型文字、「灯」分类统计与房间分区归属随之变化；设备级全局生效（同自定义图标），控制仍走原生服务，HA 零改动
- **低电量汇总**：任一设备电池 ≤20%（或电池告警触发）时，首页出现「电量不足」分类胶囊，点击查看全部低电量设备（含最低电量摘要）
- **影音控制升级**：媒体播放器浮窗新增 上一曲/播放暂停/下一曲/静音 控制排、音量滑条与**信号源切换**（按设备实际能力显示），当前播放标题展示
- **设备最近活动**（对齐 HomeKit 配件活动记录）：任意设备浮窗新增「最近活动」折叠区——本站操作记录（含操作人与动作详情）+ HA 日志簿状态变化，24 小时时间线合并倒序
- **门铃事件响应**：识别 doorbell 事件实体（或名称含「门铃」），按铃时顶部滑入横幅（同设备/同房间摄像头快照 + 一键查看实时画面，20 秒自动消失）；个人菜单可开启「浏览器通知」，页面在后台标签时补发系统通知

## 1.1.3 · 2026-07-10

### 新增
- **设备功能项按用户配置显示**：控制浮窗 → 齿轮 → 设备设置新增「显示的功能」——该设备支持的控制项逐项开关（空调各模式/风速调节、风扇预设模式/摇摆、加湿器模式、热水器运行模式、灯的色温/颜色面板），关掉即从浮窗收起；只对当前用户生效，随时恢复，「关闭」模式恒定保留
- **情景主页显示开关**：自动化页每个情景卡新增眼睛按钮，可将不常用情景从首页场景区隐藏（仍可在自动化页执行）；按用户生效

### 修复
- 深色浮窗内偏好行（显示的功能 / 摇摆送风）文字对比度不足看不清

## 1.1.2 · 2026-07-10

### 新增
- **「金门 · Golden Gate」主题**：按 macOS 27 Golden Gate 设计规范打造的第七套主题——着色玻璃（不透明基线上调，任何浓度档位文字都清晰）、全局统一 20px 圆角、加深边缘描边 + 更亮顶部镜面高光、侧栏延伸贴边、弹层阴影分级；支持玻璃浓度滑杆，金门大桥金色时刻的雾蓝→暖金壁纸（明暗双版）
- **热水器完整支持**：卡片图标即开关、状态显示「加热至 X° · 当前 Y°」；控制浮窗专属面板（开关拨杆 + 水温加减 + 运行模式按钮）；不支持原生开关服务的机型（如部分米家/云米）自动翻译为运行模式切换，行为与 HA 原生一致
- **灯组（一个设备多盏灯）**：双模灯 / 主灯+夜灯+氛围灯等自动合并为一张「N/M 开启」卡；浮窗为组级调光调色面板——按组内功能最全的灯显示，能力取并集（色温范围合并），亮度/色温/颜色按每盏灯实际能力分发；各灯独立行可点击进入单灯浮窗精细调节
- **`SESSION_COOKIE_NAME` 环境变量**：同一主机跑多套实例（不同端口）时各配一个会话 Cookie 名，解决浏览器 Cookie 不区分端口导致的互相挤登出

## 1.1.1 · 2026-07-07

### 新增
- **多 HA 实例家庭隔离**：添加 HA 实例自动创建同名家庭并绑定；同步后各实例的设备/房间自动归入对应家庭，默认家庭圈定默认实例——默认一个家庭一个 HA 实例，跨实例控制请手动新建家庭挑选设备。旧版本添加的实例在下次同步时自动补建家庭（自愈）
- **情景/自动化按家庭隔离**：列表、创建、编辑、删除、执行全部跟随当前家庭所属的 HA 实例路由；跨实例引用设备直接拒绝
- **管理端设备列表「HA 实例」筛选**：多实例来源时出现，按默认 HA / 各实例名过滤
- **多选批量操作**：我的设备（批量首页显示/隐藏）与管理端实体管理（批量隐藏/恢复）支持勾选多个实体后统一操作
- **PUID/PGID 支持**：镜像启动时自动修正数据目录属主后降权运行——`/data` 绑定宿主机目录不再因 root 属主导致 API 启动失败；可用 PUID/PGID 指定运行身份（默认 10001）

### 修复与适配
- **Zigbee2MQTT 配置项降级**：同步 HA 实体注册表的 `entity_category`，config/diagnostic 实体（Indicator 指示灯、Power outage memory 断电记忆、灵敏度校准、设备温度/电压等）不再抢主控、不计开关数、不参与全开全关与总开关级联；控制项收进「设备配置」折叠区，诊断读数进「更多信息」
- **分区型人在传感器**（小米人在传感器Pro 等）：一个设备下 N 个分区占用实体不再拆成 N 张重复卡，合并为一张（任一分区有人即「有人」，多区显示「有人 · N 区」）；多路网关（各路前缀不同）拆分行为不变
- **纯传感器设备主控**：门磁/水浸/燃气/烟雾等安全类二元传感器优先于设备温度等诊断项作为卡片主体

## 1.1.0 · 2026-07-07

### 新增
- **能源页**（一级导航）：电 / 燃气 / 水 三类历史账本，日 / 周 / 月 / 自定义范围（快捷项 + 起止日期），构成环 + 按设备堆叠柱状图 + 设备明细，费用估算（管理端可配电/气/水单价）；数据走 HA 长期统计（能源仪表盘同源），多 HA 实例自动分组
- **表计层级**：总表 → 分表 → 挂接设备的树形分账（管理端可视化编辑，防成环）；能源页按层级逐级下钻（面包屑导航），父表减下级差值显示为「未计量」；把某块表配置给用户即成为其能源页根节点，权限边界与计量边界合一
- **燃气表 / 水表配置**：与电表一致的三级作用域（全局 / 家庭 / 用户），配置的表计无条件纳入统计（不依赖实体属性）
- **来源智能识别**：kWh 单位兜底识别、同设备语义去重（保留双回路 CT1/CT2 各自计入，剔除「今日/昨日/本月」周期计数器防重复计数）

### 调整
- 底部标签与侧栏导航：首页 / 能源 / 自动化；「我的设备」移入个人菜单；首页「用电」胶囊直达能源页
- 同步清理连带处理表计（实体删除时其表计与挂接关系一并清理，下级升为顶层）

## 1.0.3 · 2026-07-06

### 新增
- **照度进大数字区**：带照度的设备（人体存在、温湿度传感器等）在浮窗顶部与温湿度并列显示大数字照度
- **设备识别对照文档**（docs/device-recognition.md）：web 识别的每种设备形态对应 HA 的哪些域 / device_class / attributes，以及识别不对时在 HA 侧的纠正方法——改完重新同步即自动纠正

## 1.0.2 · 2026-07-06

### 修复
- **同步双向清理**：「同步实体与房间」现在会删除 HA 端已删除的实体（连带权限、展示偏好、家庭设备集）与区域（房间及其授权，设备归入未分配）；按 HA 实例隔离，HA 异常返回空时不执行删除

### 新增
- **人体存在传感器统一识别**：occupancy/presence/motion 或名称含「人在/有人/存在」的设备，以存在状态为主控（指示灯开关、灵敏度等配置项不再抢卡片），状态统一显示「有人 / 无人 · 照度」，默认运动传感器图标
- **多路存在网关拆分**：一个设备下多组「XX人在」（涂鸦 Matter 网关等）按名称前缀自动拆成多张独立存在卡，路数不限
- **配置项折叠**：存在类设备浮窗中，开关/调节/一键操作收进默认折叠的「设备配置」条——这类设备设好就不常动

## 1.0.1 · 2026-07-06

### 新增
- **四套新主题**：庭院·新中式（宣纸底 + 窗棂双线框 + 朱砂印章开启态）、夜航·仪表舱（深海蓝黑 + 坐标纸网格 + 等宽数字，恒定深色）、果冻·多彩（Material You，开启整卡填色）、素墨·黑白（激活反色，唯警示留红），与原有两套共六选一
- **自定义设备图标**：186 个内置图标（MDI 115 + Phosphor 71，按类型推荐 + 全量展开）；支持上传 PNG/JPG/WebP，单色图（如自行导出的 SF Symbols）自动随主题与开关状态着色；HA 实体自带图标建议自动继承
- **用户头像**：账户设置上传/移除；侧栏、谁在家胸章（描边环示在家/外出）、成员列表全局生效
- **HomeKit 房间/分类双视图**：房间页顶部状态摘要条（温湿度/空气质量/安防/各类型开启数/门磁/动作/有人/照度）+ 设备按类别分区；分类页聚合指标 + 按房间分组
- **HomeKit 式主控开关**：竖长拨杆，设备图标即拨杆拇指（关 = 底部图标井，开 = 功能色滑至顶部）；安防面板改竖排模式列
- **风机能力扩展**：预设模式（换气/干燥/取暖等，自动中文化）与摇摆送风——浴霸、新风、循环扇开箱即用
- **智能组合形态**：插排每个端口行内显示对应功率/电流（名称/编号自动匹配）；冰箱优先显示实际温度、任一门未关卡片提醒；3D 打印机（Bambu 等）卡片显示打印状态·进度·剩余时间
- **实体响应状态筛选**：「我的设备」与管理后台均可按 正常/无响应 过滤（带计数、红点标识），无响应实体可一键批量隐藏
- **一体化镜像**：`jeesa/tongtang` 单容器部署（FastAPI 托管前端，无需 Nginx/compose）

### 优化
- 性能：WebSocket 推送 300ms 批量合并；设备卡片记忆化（仅重绘状态变化的卡）；传感器折线图与摄像头快照进入视口才加载；页面隐藏时暂停摄像头轮询；瓷贴移除逐卡模糊滤镜
- 组合浮窗：「更多信息」默认折叠；小型全开/全关胶囊替代巨型圆钮；组合主控开关联动全部可开关成员
- 浮窗内容超高时整体滚动、标题钉顶；管理后台移动端布局修复；主题按钮文字居中

### 修复
- fan 控制指令（模式/摇摆/风速）改为可组合下发
- 组合卡在实时推送下不再解散；素墨主题黑底黑字修复

## 1.0.0 · 2026-07-06

首个发布版本：多家庭/多用户/多 HA 实例的 Home Assistant 家庭权限控制台，HomeKit 风格界面，Docker Hub 多架构镜像（amd64/arm64），镜像内不含可读源码。
